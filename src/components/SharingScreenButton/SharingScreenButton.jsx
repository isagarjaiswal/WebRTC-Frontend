import React from "react";
// import "./SharingScreenButton.css";
import { Button } from "../Common/Button";

export const SharingScreenButton = ({ onClick }) => {
  return <Button onClick={onClick}>ShareScreen</Button>;
};
